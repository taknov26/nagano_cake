<h2>ジャンル一覧・追加</h2>
  <%= form_with model: @genre, url: admins_genres_path, local:true do |f| %>
<table>
  <tbody>
    <tr>
      <th>ジャンル名</th>
      <th><%= f.text_field :name %></th>
      <th><%= f.submit '新規登録' %></th>
    </tr>
  </tbody>
</table>
<% end %>