<div class="container mt-5">
  <h5 class="d-inline mt-10 bg-light">ログイン</h5>
    <div class="row">
      <div class="mx-auto col-lg-6 mt-4">
        <h6 class="d-inline mt-2 bg-light">会員の方はこちらからログイン</h6>
        <%= form_with model: @customer, url: customer_session_path, local:true do |f| %>
        <table class="mt-3">
          <tbody>
            <tr>
              <td><%= f.label "メールアドレス" %></td>
              <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
            </tr>
            <tr>
              <td><%= f.label "パスワード" %></td>
              <td><%= f.password_field :password, autocomplete: "current-password" %></td>
            </tr>
          </tbody>
        </table>
        <div class="mt-2 ml-10">
            <%= f.submit "ログイン", class: 'btn btn-primary' %>
        <% end %>
        </div>
      </div>
    </div>
    <div class="row mt-3">
      <div class="col-lg-6 mx-auto">
        <h6 class="d-inline mt-2 bg-light">会員登録がお済みでない方</h6>
        <p class="mt-1"><%= link_to new_customer_registration_path do %>
              こちら
              <% end %>
              から新規登録を行ってください。</p>
      </div>
    </div>
</div>

