<h3>注文情報入力</h3>
<%= form_with model: @order, url: public_orders_confirm_path method: :post, local:true do |f| %>
<h4>支払方法</h4>
<%= f.radio_button :payment, "credit" %>クレジットカード</br>
<%= f.radio_button :paymant, "bank"　%>銀行振込
<h4>お届け先</h4>
<%= f.radio_button :postal_code, :address, :orders_name, @customer.postal_code, @customer.address, @customer.name %>ご自身の住所</br>
<%= @customer.postal_code %><%= @customer.address %><%= @customer.name %>
<%= f.radio_button :postal_code, :address, :orders_name, @address.postal_code, @address.address, @address.name %>登録済住所から選択
<%= 